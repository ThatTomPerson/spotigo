project_name: spotigo

builds:
  - main: cmd/spotify/main.go
    binary: spotify
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - 386
  - main: cmd/spotifyd/main.go
    binary: spotifyd
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - 386
brew:
  # Reporitory to push the tap to.
  github:
    owner: ThatTomPerson
    name: homebrew-tap

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^refactor'